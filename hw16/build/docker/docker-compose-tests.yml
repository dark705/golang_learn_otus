version: "3.7"
services:
  godog:
    build:
      context: ../../
      dockerfile: ./build//docker/godog/Dockerfile
    command: sh -c 'cd tests && godog'
    networks:
      - calendar
networks:
  calendar:





