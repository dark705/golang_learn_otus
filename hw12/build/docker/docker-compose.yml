version: "3.7"
services:
  postgres:
    image: postgres:11
    #volumes:
     # - ./data:/var/lib/postgresql/data
    ports:
      - "5444:5432"
    environment:
      POSTGRES_USER: tmp
      POSTGRES_PASSWORD: tmp
      POSTGRES_DB: calendar