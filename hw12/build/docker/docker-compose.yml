version: "3.7"
services:
  postgres:
    image: postgres:11
    #volumes:
     # - ./data:/var/lib/postgresql/data
    ports:
      - "54441:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: calendar
